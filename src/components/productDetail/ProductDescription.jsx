import React from 'react'

const ProductDescription = ({productInfo}) => {
  return (
    <section className='product-desc'>
        <h2 className='product-desc__name'>{}</h2>
    </section>
  )
}

export default ProductDescription